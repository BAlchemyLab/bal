# dev env install:
pip install pipenv
pipenv --python=python3.6
pipenv install
echo "PYTHONPATH=bal" > .env
pipenv shell
python setup.py install


# export PYTHONPATH=bal


install mininet (try first with package if not work then install from source)

# mininet VM install:
python setup.py install
easy_install dist/bal-xxx.egg
sudo ln -s /home/mininet/bal/bal/blockchain.py /usr/local/bin/blockchain.py
change #!/bin/env python in blockchain.py to #!usr/bin/env python
